import 'package:flutter/widgets.dart';

class ElegantAlertDialog extends StatelessWidget {
  const ElegantAlertDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
